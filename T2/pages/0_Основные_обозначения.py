import streamlit as st

st.title("Основные обозначения")

st.markdown(r""" 
| как | $\rightarrow$ | что означает |
| ----------------------: | :------: | :------------------------------ |
| $x = \{x_i\}_{i=1}^n$ | $\rightarrow$ | $n$-мерный вектор неизвестных |
| $x^k,\, x^{k+1}$ | $\rightarrow$ | приближения решения на итерациях $k$ и $k+1$ |
| $A = \{a_{ij}\}$ | $\rightarrow$ | матрица коэффициентов системы |
| $A x = f$ | $\rightarrow$ | линейная система уравнений |
| $D = \mathrm{diag}(a_{11},\dots,a_{nn})$ | $\rightarrow$ | диагональная часть матрицы $A$ |
| $L$ | $\rightarrow$ | нижняя треугольная часть матрицы $A$ |
| $U$ | $\rightarrow$ | верхняя треугольная часть матрицы $A$ |
| $(x,y)=\sum_{i=1}^n x_i y_i$ | $\rightarrow$ | скалярное произведение |
| $\|x\| = \sqrt{(x,x)}$ | $\rightarrow$ | евклидова норма |
| $r^k = f - Ax^k$ | $\rightarrow$ | вектор невязки на итерации $k$ |
| $p^k$ | $\rightarrow$ | направление спуска в методе сопряжённых градиентов |
| $\alpha_k$ | $\rightarrow$ | шаг вдоль направления спуска |
| $\beta_k$ | $\rightarrow$ | коэффициент обновления направления (Флетчер — Ривс) |
| $\lambda_i$ | $\rightarrow$ | собственные значения матрицы $A$ |
| $x^T A x$ | $\rightarrow$ | квадратичная форма (критерий положительной определённости) |
| $a_{i,i+1},\, a_{i,i-1}$ | $\rightarrow$ | элементы трёхдиагональной матрицы из задачи 2.1 |
| $H_n = \left\{\frac{1}{i+j-1}\right\}$ | $\rightarrow$ | матрица Гильберта размера $n$ |
| $\tau_{k+1}$ | $\rightarrow$ | параметр шага в канонической формуле Самарского |
| $n$ | $\rightarrow$ | размерность задачи |

""")
