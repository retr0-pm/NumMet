import streamlit as st
import matplotlib.pyplot as plt
import numpy as np

st.set_page_config(page_title="Matplotlib", layout="wide")

menu = st.sidebar.radio("***",
    (
        "Общая характеристика ПО",
        "Архитектура",
        "Преимущества и недостатки",
    )
)

# -------------------- ОБЩАЯ ХАРАКТЕРИСТИКА --------------------
if menu == "Общая характеристика ПО":
    st.markdown("""
    #### 🎨 Общая характеристика Matplotlib

    **Matplotlib** — это мощная библиотека Python для **создания графиков и визуализации данных**.  
    Она является стандартом де-факто для построения 2D-графиков и активно используется в:
    * научных исследованиях,
    * инженерных приложениях,
    * машинном обучении,
    * аналитике данных и отчётности.

    **Ключевые особенности:**
    * предоставляет удобный интерфейс `matplotlib.pyplot`, напоминающий MATLAB;
    * поддерживает десятки типов графиков — от простых линий до сложных 3D-поверхностей;
    * полностью настраиваема: подписи, цвета, сетки, легенды, шрифты, аннотации;
    * интегрируется с Jupyter, NumPy, Pandas и другими библиотеками.
    """)

    st.divider()
    st.markdown("#### 📈 Пример простого графика")

    x = np.linspace(0, 2 * np.pi, 200)
    y = np.sin(x)
    fig, ax = plt.subplots(figsize=(5, 3))  # фиксированный размер
    ax.plot(x, y, color='royalblue', linewidth=2)
    ax.set_title("Пример: $y = \sin(x)$", pad=12)
    ax.set_xlabel("$x$")
    ax.set_ylabel("$y$")
    ax.grid(True, linestyle='--', alpha=0.6)
    st.pyplot(fig, use_container_width=False)

    st.caption("Matplotlib превращает численные данные в наглядные изображения.")

# -------------------- АРХИТЕКТУРА --------------------
if menu == "Архитектура":
    st.markdown("""
    #### 🧩 Архитектура Matplotlib

    Архитектура Matplotlib построена по **многоуровневому принципу**,  
    что делает её гибкой и адаптируемой под разные среды (скрипты, ноутбуки, GUI-приложения и т.д.).
    """)

    st.markdown("""
    **Основные уровни архитектуры:**
    1. **Pyplot API** — верхний уровень, предназначенный для быстрого построения графиков (интерфейс команд, похожих на MATLAB).
    2. **Artist Layer** — слой объектов (линии, тексты, оси, фигуры), из которых состоит график.
    3. **Backend Layer** — отвечает за отрисовку (на экране или в файл).  
       Поддерживаются различные бэкенды: `Agg`, `TkAgg`, `Qt5Agg`, `SVG`, `PDF` и др.

    🔹 **Верхний уровень (Pyplot)** — простой и интуитивный.  
    🔹 **Нижний уровень (Artist, Backend)** — даёт полный контроль над деталями графика.
    """)

    st.caption("Каждый элемент графика — это объект (`Artist`), управляемый слоями архитектуры Matplotlib.")

# -------------------- ПРЕИМУЩЕСТВА И НЕДОСТАТКИ --------------------
if menu == "Преимущества и недостатки":
    st.markdown("""
    #### ⚖️ Преимущества и недостатки Matplotlib
    """)

    col1, col2 = st.columns(2)

    with col1:
        st.markdown("""
        ##### ✅ Преимущества
        * Богатый функционал для 2D-визуализации.
        * Поддержка десятков форматов вывода (PNG, PDF, SVG, EPS).
        * Полный контроль над элементами графика.
        * Совместимость с **NumPy**, **Pandas**, **SciPy**.
        * Поддержка интеграции с GUI-фреймворками (Tkinter, Qt, wxPython).
        * Готовые стили и темы оформления (`plt.style.use()`).
        * Огромное сообщество и документация.
        """)

    with col2:
        st.markdown("""
        ##### ⚠️ Недостатки
        * Относительно сложный синтаксис при детальной настройке.
        * Меньшая интерактивность по сравнению с **Plotly** или **Bokeh**.
        * Ограниченные 3D-возможности.
        * Для сложных визуализаций — необходимость в большом объёме кода.
        """)

    st.divider()
    st.markdown("#### 🧭 Вывод")
    st.success("""
    Matplotlib — это фундамент визуализации данных в Python.  
    Несмотря на существование более современных инструментов,  
    именно Matplotlib остаётся основой большинства графических библиотек и используется повсеместно.
    """)
