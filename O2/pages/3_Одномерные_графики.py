import streamlit as st
import matplotlib.pyplot as plt
import numpy as np

st.set_page_config(page_title="Одномерные графики", layout="wide")

menu = st.sidebar.radio("***",
    (
        "Общее определение принадлежности графика к одномерным",
        "Линейные графики",
        "Точечные диаграммы",
        "Столбчатые диаграммы",
        "Гистограммы распределений",
        "Ступенчатые графики",
    )
)

# -------------------- ОБЩЕЕ ОПРЕДЕЛЕНИЕ --------------------
if menu == "Общее определение принадлежности графика к одномерным":
    st.markdown("""
    #### 📊 Общее определение принадлежности графика к одномерным

    **Одномерные графики** — это визуализации, которые отображают зависимость **одной переменной** 
    от другой (обычно по оси X и оси Y).  
    Они используются для представления:
    * изменения значений по времени,
    * сравнения величин,
    * анализа распределений.

    Такие графики представляют данные в виде точек, линий или столбцов, 
    где ось X показывает **индекс или категорию**, а ось Y — **величину**.
    """)

    st.info("""
    Примеры одномерных графиков:  
    🔹 линейный график  
    🔹 точечная диаграмма  
    🔹 столбчатая диаграмма  
    🔹 гистограмма распределения  
    🔹 ступенчатый график
    """)

    st.caption("Все эти типы графиков позволяют исследовать зависимость одной переменной от другой.")

# -------------------- ЛИНЕЙНЫЕ --------------------
if menu == "Линейные графики":
    st.markdown("""
    #### 📈 Линейные графики

    **Линейный график** показывает, как изменяется величина с течением времени или по индексам.
    Он соединяет точки линией, что делает тренд визуально очевидным.
    """)

    x = np.linspace(0, 10, 100)
    y1 = np.sin(x)
    y2 = np.cos(x)

    fig, ax = plt.subplots()
    ax.plot(x, y1, label="sin(x)", color="royalblue", linewidth=2)
    ax.plot(x, y2, label="cos(x)", color="tomato", linestyle="--", linewidth=2)
    ax.set_xlabel("x")
    ax.set_ylabel("f(x)")
    ax.set_title("Линейные графики функций sin(x) и cos(x)")
    ax.legend()
    ax.grid(True, linestyle="--", alpha=0.6)
    st.pyplot(fig)

    st.caption("Линейные графики подходят для отображения трендов и временных рядов.")

# -------------------- ТОЧЕЧНЫЕ --------------------
if menu == "Точечные диаграммы":
    st.markdown("""
    #### 🔵 Точечные диаграммы (Scatter Plot)

    **Точечная диаграмма** показывает зависимость между двумя числовыми переменными.  
    Каждый элемент данных представлен в виде отдельной точки.
    """)

    np.random.seed(0)
    x = np.random.rand(50)
    y = 2 * x + np.random.randn(50) * 0.2

    fig, ax = plt.subplots()
    ax.scatter(x, y, color="mediumseagreen", edgecolor="black", s=70)
    ax.set_xlabel("x")
    ax.set_ylabel("y")
    ax.set_title("Пример точечной диаграммы")
    ax.grid(True, linestyle="--", alpha=0.6)
    st.pyplot(fig)

    st.caption("Используется для выявления закономерностей, трендов и выбросов в данных.")

# -------------------- СТОЛБЧАТЫЕ --------------------
if menu == "Столбчатые диаграммы":
    st.markdown("""
    #### 🟩 Столбчатые диаграммы (Bar Chart)

    **Столбчатая диаграмма** используется для сравнения категориальных данных.  
    Высота каждого столбца отображает значение соответствующей категории.
    """)

    categories = ['A', 'B', 'C', 'D', 'E']
    values = [23, 17, 35, 29, 12]

    fig, ax = plt.subplots()
    ax.bar(categories, values, color='cornflowerblue', edgecolor='black')
    ax.set_title("Пример столбчатой диаграммы")
    ax.set_xlabel("Категории")
    ax.set_ylabel("Значение")
    st.pyplot(fig)

    st.caption("Столбчатые графики полезны для сравнения категорий и групп данных.")

# -------------------- ГИСТОГРАММЫ --------------------
if menu == "Гистограммы распределений":
    st.markdown("""
    #### 📊 Гистограммы распределений

    **Гистограмма** показывает, как распределены значения числовой переменной.  
    Она разбивает диапазон на интервалы (боксы) и показывает, сколько значений попадает в каждый из них.
    """)

    data = np.random.randn(500)
    fig, ax = plt.subplots()
    ax.hist(data, bins=20, color="orchid", edgecolor="black", alpha=0.8)
    ax.set_title("Пример гистограммы распределения")
    ax.set_xlabel("Значение")
    ax.set_ylabel("Частота")
    st.pyplot(fig)

    st.caption("Гистограммы позволяют выявлять форму распределения и наличие выбросов.")

# -------------------- СТУПЕНЧАТЫЕ --------------------
if menu == "Ступенчатые графики":
    st.markdown("""
    #### 📶 Ступенчатые графики (Step Plot)

    **Ступенчатый график** используется, когда значения меняются дискретно,  
    например при изменении уровня сигнала, тарифов или состояний системы.
    """)

    x = np.arange(10)
    y = np.random.randint(0, 5, size=10)

    fig, ax = plt.subplots()
    ax.step(x, y, where="mid", color="darkorange", linewidth=2)
    ax.set_xlabel("Номер измерения")
    ax.set_ylabel("Значение")
    ax.set_title("Пример ступенчатого графика")
    ax.grid(True, linestyle="--", alpha=0.6)
    st.pyplot(fig)

    st.caption("Ступенчатые графики удобны для отображения дискретных изменений и пороговых состояний.")
