import streamlit as st
import segno
from io import BytesIO

st.set_page_config(page_title="Заключение", layout="centered")

# -------------------- МЕНЮ --------------------
menu = st.sidebar.radio("Разделы",
                        ("Обзор и возможности", "Заключение"))

# -------------------- ОБЗОР И ВОЗМОЖНОСТИ --------------------
if menu == "Обзор и возможности":
    st.markdown("""
    #### 🎓 Обзор и основные возможности Matplotlib

    **Matplotlib** — это базовый инструмент для визуализации данных в Python.  
    Он сочетает **гибкость**, **надёжность** и **универсальность**, обеспечивая основу для большинства современных библиотек визуализации (Seaborn, Plotly, Pandas plotting и др.).

    **Основные возможности:**
    * Работа с **разными типами данных** — от одномерных массивов до многомерных полей.  
    * Построение **2D и 3D графиков**, включая поверхности, контуры, сетки, векторные поля и диаграммы рассеяния.  
    * Тонкая настройка всех элементов визуализации — цвета, подписи, масштаб, углы обзора.  
    * Кроссплатформенность и воспроизводимость — идеально для научных и инженерных задач.
    """)

# -------------------- ЗАКЛЮЧЕНИЕ --------------------
elif menu == "Заключение":
    st.markdown("""
    #### 💬 Заключение

    **Matplotlib** — это не просто библиотека, а **фундамент визуальной аналитики в Python**,  
    обеспечивающий исследователей, разработчиков и инженеров мощным инструментом для наглядного представления данных.

    ✅ Универсальный инструмент визуализации — подходит как для простых графиков, так и для сложных 3D сцен.
    """)

    # -------------------- ССЫЛКА И QR-КОД --------------------
    st.markdown("#### 🔗 Проект на GitHub")
    url = "https://github.com/retr0-pm/NumMet"
    st.markdown(f"[Перейти на страницу проекта]({url})")

    # Генерация QR-кода через segno
    qr = segno.make(url)
    buffer = BytesIO()
    qr.save(buffer, kind='png', scale=6)
    buffer.seek(0)
    st.image(buffer, caption="QR-код проекта на GitHub", use_container_width=False)
